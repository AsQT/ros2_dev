# robot_gui

C++/Qt robot GUI with native embedded RViz through `rviz_common`.

Run:

```bash
ros2 launch robot_gui robot_gui.launch.py embed_rviz:=true
```

The old Python/PyQt implementation is kept as `robot_gui_old` and ignored by colcon.
