# robot_gui config

The embedded RViz view defaults to `robot_moveit/config/moveit.rviz`.
Override it with the launch parameters:

- `embed_rviz`
- `rviz_config_package`
- `rviz_config_relative_path`
